import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/stockify/', // Imposta il percorso di base per il deployment su GitHub Pages
})

